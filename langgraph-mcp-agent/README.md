# langgraph-mcp-agent

A LangGraph agent with a mock MCP tool server for file system and database operations.

## Setup

```bash
pip install -r requirements.txt
```

## Run tests

```bash
pytest tests/
```
